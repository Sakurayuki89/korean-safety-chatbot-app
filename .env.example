# 🔧 Korean Safety Equipment Management System - Environment Variables
# Copy this file to .env.local and fill in your actual values

# === Google API Configuration ===
# Get these from Google Cloud Console
GOOGLE_CLIENT_ID=your_google_oauth_client_id
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret
GOOGLE_REDIRECT_URI=https://korean-safety-chatbot-app.vercel.app/api/google/auth/stateless/callback

# Google Service Account JSON (for Sheets and Drive API)
GOOGLE_APPLICATION_CREDENTIALS='{"type":"service_account","project_id":"your-project",...}'

# Google Sheets and Drive IDs
GOOGLE_SHEETS_SPREADSHEET_ID=your_google_sheets_id
GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id

# === Database Configuration ===
# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/korean-safety-system

# === AI Configuration ===
# Google Gemini API key
GEMINI_API_KEY=your_gemini_api_key

# === Application Configuration ===
# Environment (development, production)
NODE_ENV=development

# Next.js configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_key

# === Optional Configuration ===
# For production deployment
NEXT_PUBLIC_APP_URL=https://yourdomain.com

# === Admin Authentication ===
# Simple password-based admin authentication
ADMIN_PASSWORD=your_secure_admin_password

# JWT secret for session management
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure

# === Security Notes ===
# 1. Never commit .env.local to version control
# 2. Use strong secrets for production (min 32 characters)
# 3. Rotate API keys regularly
# 4. Use environment-specific values for each deployment
# 5. CRITICAL: Never hardcode secrets in vercel.json or documentation files
# 6. Set GOOGLE_REDIRECT_URI to match your domain exactly
# 7. Verify Google Cloud Console OAuth settings match redirect URI

# === Troubleshooting Tips ===
# • GitHub Push Protection: Remove secrets from code, use GitHub Secret Scanning allowlist
# • Google OAuth 400 Error: Check GOOGLE_REDIRECT_URI matches Google Console exactly
# • See TROUBLESHOOTING.md for detailed problem resolution steps